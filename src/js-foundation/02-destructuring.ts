//console.log(process);

export const {SHELL, HOMEBREW_PREFIX, npm_lifecycle_script} = process.env;

//console.log({SHELL, HOMEBREW_PREFIX, npm_lifecycle_script});

const characters = ['Goku', 'Vegeta', 'Green Lantern', 'Batman'];

const [, , ,batman] = characters;

//console.log(batman);